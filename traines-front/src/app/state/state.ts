import {signal} from "@angular/core";

export const isHomeActive = signal<boolean>(false);

export const isTicketsActive = signal<boolean>(false);

export const isRouteActive = signal<boolean>(false);
